<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:orientation="vertical"
    android:padding="16dp"

    tools:context=".ActivityReminderslayout">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="#1F1A1A"
        android:gravity="center_vertical">

        <!-- Start Drawable (Icon) -->
        <ImageView
            android:id="@+id/iconStart"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/search_svgrepo_com"
            android:layout_marginEnd="8dp" />

        <!-- Search Bar -->
        <EditText
            android:id="@+id/searchBar"
            android:layout_width="0dp"
            android:layout_height="50dp"
            android:layout_weight="1"
            android:background=""
            android:hint="Search"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#AAAAAA" />

        <!-- End Drawable (Mic Icon) -->
        <ImageView
            android:id="@+id/iconEnd"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/microphone_svgrepo_com__1_"
            android:layout_marginStart="8dp" />
    </LinearLayout>


</LinearLayout>